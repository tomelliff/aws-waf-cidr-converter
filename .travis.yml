language: python
python:
- '2.7'
install:
- pip install -r requirements-dev.txt
- pip install coveralls
script:
- coverage run --source=converter -m unittest discover
- flake8 --exclude .git,*.pyc,env
after_success:
- coveralls
